
rin
.controller('PageHelpCtrl', [
    '$scope',
    'ngProgress',
    function ($scope, ngProgress) {
        ngProgress.complete();
    }
])
.controller('PageTellusCtrl', [
    '$scope',
    'ngProgress',
    function ($scope, ngProgress) {
        ngProgress.complete();
    }
]);
